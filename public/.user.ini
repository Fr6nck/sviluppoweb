; Arco del Vento — limiti di PHP per questa cartella.
; Le foto del telefono pesano da 3 a 8 MB: con il limite di partenza di
; alcuni server (2 MB) l'area Immagini le rifiuterebbe. Il sito ne accetta
; fino a 15 MB e le riduce da sé. Se il server non legge questo file, il
; limite resta quello del pannello di Hostinger e l'area Immagini lo dice.
upload_max_filesize = 16M
post_max_size = 20M
